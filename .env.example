# Database Configuration
# Note: DATABASE_URL, HOST, PORT, and RUST_LOG are set in process-compose.yml
# Only override if running backend outside of process-compose
# DATABASE_URL=postgres://postgres:password@localhost:5432/database_name
# HOST=0.0.0.0
# PORT=3000
# RUST_LOG=info,backend=debug

# AWS Configuration (required)
AWS_PROFILE=your-aws-profile
AWS_REGION=us-east-1

# AWS SES Configuration (required)
SES_FROM_EMAIL=noreply@example.com
SES_REPLY_TO_EMAIL=contact@example.com

# JWT Configuration (required - generate with: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here
